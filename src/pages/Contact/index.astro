---
import { ContactBanner } from "@utils/images";
import {
  BackGroundImage,
  HTML,
  FreeConsultation,
  FormContact,
  Heading,
} from "@utils/Components";
---

<Fragment>
  <HTML>
    <BackGroundImage imageSource={ContactBanner} text="CONTACT" />
    <div id="contact-items"></div>
    <section class="lg:container lg:mx-auto">
      <div
        class="grid lg:grid-cols-2 lg:mx-0 md:mx-0 sm:mx-0 lg:py-10 sm:py-0 sm:pb-10"
        id="free-consultation"
      >
        <FreeConsultation />
        <div
          class="flex flex-col col-span-1 justify-between xl:px-[50px] lg:px-[40px] lg:mx-[0px] md:mx-0 sm:mx-6 lg:border-l lg:border-t-0 md:border-t sm:border-t border-semigray"
        >
          <Heading
            Heading="SEND US A MESSAGE"
            id="free-consultation"
            style="text-center lg:pt-0 md:mb-10 md:pt-0 sm:pt-10 md:pb-0 lg:mt-0 md:mt-10 sm:pb-6"
          />
          <FormContact client:visible />
        </div>
        <!-- <section
          class="flex sm:flex-col xl:px-[50px] lg:px-[40px] items-center md:w-full sm:wi-full lg:py-0 md:py-10 sm:py-0 lg:pt-0 sm:pt-16 lg:border-l-[0.1px] border-lightgray"
        >
          <div
            class="xl:mx-[100px] lg:mx-[50px] md:w-full sm:w-[90%]"
            id="send-message"
          >
            <h2
              class="lg:text-4xl md:text-2xl sm:text-2xl font-[Poppins] text-center font-black lg:pb-6 md:pb-14 sm:pb-14"
            >
              SEND US A MESSAGE
            </h2>
            <div class="">
              <FormContact client:visible />
            </div>
          </div>
        </section> -->
      </div>
    </section>
    <script src="https://smtpjs.com/v3/smtp.js"></script>
  </HTML>
</Fragment>
